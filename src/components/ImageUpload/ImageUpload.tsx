import * as IU from './ImageUpload.sytles'

export interface ImageUploadProps {
    label: string
  }
  
  function ImageUpload (props: ImageUploadProps) {
    return <IU.Wrapper>
          todo: image upload
        </IU.Wrapper>
    
  }
  export default ImageUpload;
  
